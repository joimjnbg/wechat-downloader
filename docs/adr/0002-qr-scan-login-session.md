# QR scan captures the login session cookie for full history sync

Whole-account sync needs WeChat login state for the history API. Public-only parsing misses older articles; manual cookie paste is error-prone. The user scans a QR in the WeChat app, the tool saves the session cookie and reuses it — easy flow with full history access.
