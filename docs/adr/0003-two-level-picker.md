# Two-level picker: batch type toggles plus per-article checklist

A synced Account can hold hundreds of articles, so per-article ticking alone does not scale, and type-only filtering cannot exclude individual posts. v1 offers both: batch toggles for text/images/audio/video applied to all articles, with a per-article checklist to include or exclude specific ones.
